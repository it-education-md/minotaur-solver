# Miner solver dependencies.
#
# Keep this file empty unless the dependency is vendored or already available
# in ghcr.io/subnet112/solver-base:v1. Validator screening builds with
# --network=none, so pip cannot download packages from PyPI.
